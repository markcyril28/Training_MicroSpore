name: micro
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - numpy
  - pip:
    - pyyaml
    - psutil  # For RAM detection in dataset caching
    - pytest  # For running tests
    # PyTorch with ROCm is installed separately via setup_conda_server.sh
